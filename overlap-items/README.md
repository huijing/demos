# Overlapping Grid Items

One of the cool things about CSS grid is we now have a legit way to overlap items. For the longest time, overlapping elements involved absolute positioning which was tricky because it took the element out of the document flow and things wouldn't align properly and it was just too much trouble. But now, grid placement properties let's us layer elements if we so choose, and they all still participate in the document flow.
